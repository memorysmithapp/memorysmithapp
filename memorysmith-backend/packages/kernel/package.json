{
  "name": "@memorysmith/kernel",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Shared kernel: SubscriptionId, Ulid, Slug, Position, Authorship, Result, DomainEvent and the error taxonomy. Deliberately tiny — a large shared kernel is coupling in disguise.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  }
}
