{
  "name": "@memorysmith/markdown-spec",
  "private": true,
  "description": "The MemorySmith Markdown Specification as data, with the suite that proves the product reads it. The document it belongs to is docs/markdown-spec.md; this package is what the product implements it with. It carries no version of its own and follows the version of the product.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "test": "node tools/check-spec.mjs"
  },
  "exports": {
    "./spec.json": "./spec.json",
    "./conformance.json": "./tests/conformance.json",
    "./schema.json": "./schema/spec.schema.json"
  }
}
