{
  "name": "@memorysmith/backend",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "description": "Backend workspace root: the six bounded contexts, the shared kernel and the event contracts. Holds no code of its own; every package under packages/, services/ and apps/ extends tsconfig.base.json from here."
}
