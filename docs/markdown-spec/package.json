{
  "name": "@memorysmith/markdown-spec",
  "private": true,
  "description": "The MemorySmith Markdown Specification: the notation a note is written in, as data, with the suite that proves the product reads it. It carries no version of its own and follows the version of the product.",
  "type": "module",
  "scripts": {
    "test": "node tools/check-spec.mjs"
  },
  "exports": {
    "./spec.json": "./spec.json",
    "./conformance.json": "./tests/conformance.json",
    "./schema.json": "./schema/spec.schema.json"
  }
}
