{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://md.memorysmith.app/schema/spec.schema.json",
  "title": "MemorySmith Markdown Specification",
  "description": "The machine-readable half of the specification: every notation it declares, with its observable effect.",
  "type": "object",
  "required": ["spec", "version", "sources", "notations"],
  "additionalProperties": false,
  "properties": {
    "$schema": { "type": "string" },
    "spec": { "type": "string" },
    "version": { "type": "string", "pattern": "^\\d+\\.\\d+\\.\\d+$" },
    "variant": {
      "type": "string",
      "description": "The value of the variant parameter of text/markdown, per RFC 7763."
    },
    "url": { "type": "string", "format": "uri" },
    "sources": {
      "type": "array",
      "description": "Where the forms this specification declares were established. A source is cited per form in SPEC.md; it is not a tier the specification is built out of.",
      "items": {
        "type": "object",
        "required": ["id", "name", "url"],
        "additionalProperties": false,
        "properties": {
          "id": { "type": "string" },
          "name": { "type": "string" },
          "version": { "type": "string", "description": "Absent for a source that publishes documentation rather than a versioned specification." },
          "url": { "type": "string", "format": "uri" }
        }
      }
    },
    "notations": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": ["id", "reader", "syntax", "example", "effect"],
        "additionalProperties": false,
        "properties": {
          "id": {
            "type": "string",
            "pattern": "^[a-z0-9]+(-[a-z0-9]+)*$",
            "description": "Stable identifier. Never renamed and never reused."
          },
          "reader": {
            "enum": ["links", "frontmatter", "reading-surface"],
            "description": "Which reader decides this notation. The first two are the only readers of content an indexer has; the third is display."
          },
          "syntax": { "type": "string", "description": "The form, as an author would type it." },
          "example": { "type": "string", "description": "A body exercising the form, used verbatim by the conformance suite." },
          "effect": { "type": "string", "description": "What observably happens. Written for whoever writes a note, not for the code." },
          "spec": { "type": "string", "description": "The section or sections of SPEC.md that govern it." }
        }
      }
    }
  }
}
